package #pkg;

import #base_presenter_imp_pkg;
import #view_pkg;

/**
 * generate by MVPHelper
 */
public class #namePresenterImp extends #base_presenter_imp_name<#nameView>
        implements #namePresenter {
    public #namePresenterImp(#nameView view) {
        super(view);
    }
}